<?php
if($this->query){
    foreach ($this->matches as $match){
        $doc = $match->getDocument();
        echo $this->query->highlightMatches($match->url);
        foreach ($doc->getFieldNames() as $name){
            echo '<br/><b>'.$name .':</b> ' .$this->query->highlightMatches($doc->getFieldValue($name)).'<br/>';
        }
    }
} else {
    echo 'add q to url';
}

?>