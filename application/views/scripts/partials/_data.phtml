<table border="1">
	<tr>
		<?php
		$value = $this->data[0];
			foreach ($value as $key => $value) {
				echo "<th>".$key."</th>";}
		?>
	</tr>
	<tr>
		<?php
		foreach ($this->data as $key => $value) {
			foreach ($value as $key => $value) {
				echo "<td>".$value."</td>";
			}
			echo "</tr>";
		}
		?>
</table>